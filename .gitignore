# Personal config lives in ~/.llm-wiki — never in the repo
schema/config.yaml
schema/filter-identity.md
schema/query-log.md

# OS artifacts
.DS_Store

# Claude Code local settings
.claude/settings.local.json
